<!-- Page -->
<html>
    <head>
        <style> 
            body {
                margin: 0px;
                padding: 0px;
            }
            img {
                width: 100%;
            }
            #title {
                text-align: center;
                padding: 10px 0;
                margin: 0;
            }
            #full-banner {
                width: 100%;
                padding: 5px;
                margin: 0px;
                height: 16px;
                text-align: center;
                font-size: 12px;
                font-weight: bolder;
                background-color: #ffaa99;
                font-family: Arial;
                vertical-align: middle;
            }
            #full-banner p {
                padding: 0px;
                margin: auto;
            }
        </style>
        <script type="text/javascript" src="https://ydc-dev-ed.lightning.force.com/canvas/sdk/js/45.0/canvas-all.js"></script>
        
    </head>
    <body>

        <% 
            var user = "No user";
            if (context != "") { 
                var user = context['context']['user']['userName'];
                var contextJSON = context['context'];
            }
        %>
        <h3 id="title">Heroku Apps in Salesforce Canvas</h3>
        <div id="full-banner"><p>You're connected to this system as <%= user %>.</p></div>

        <img id="ctx" src="<%= url %>" />

        <script>
            console.log("SFDC Raw Context: <%= contextJSON %>");
        </script>

    </body>
</html>
